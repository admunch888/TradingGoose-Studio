{{- if .Values.app.enabled }}
---
# Service for main application
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradinggoose.fullname" . }}-app
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: {{ .Values.app.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tradinggoose.app.selectorLabels" . | nindent 4 }}
{{- end }}

{{- if .Values.realtime.enabled }}
---
# Service for realtime server
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradinggoose.fullname" . }}-realtime
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.realtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.realtime.service.type }}
  ports:
    - port: {{ .Values.realtime.service.port }}
      targetPort: {{ .Values.realtime.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tradinggoose.realtime.selectorLabels" . | nindent 4 }}
{{- end }}

{{- if .Values.postgresql.enabled }}
---
# Service for PostgreSQL
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradinggoose.fullname" . }}-postgresql
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.postgresql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.postgresql.service.type }}
  ports:
    - port: {{ .Values.postgresql.service.port }}
      targetPort: {{ .Values.postgresql.service.targetPort }}
      protocol: TCP
      name: postgresql
  selector:
    {{- include "tradinggoose.postgresql.selectorLabels" . | nindent 4 }}
{{- end }}

{{- if .Values.ollama.enabled }}
---
# Service for Ollama
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradinggoose.fullname" . }}-ollama
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.ollama.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ollama.service.type }}
  ports:
    - port: {{ .Values.ollama.service.port }}
      targetPort: {{ .Values.ollama.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tradinggoose.ollama.selectorLabels" . | nindent 4 }}
{{- end }}

{{- if .Values.kronos.enabled }}
---
# Service for Kronos forecasting
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradinggoose.fullname" . }}-kronos
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.kronos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.kronos.service.type }}
  ports:
    - port: {{ .Values.kronos.service.port }}
      targetPort: {{ .Values.kronos.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tradinggoose.kronos.selectorLabels" . | nindent 4 }}
{{- end }}