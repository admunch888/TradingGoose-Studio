{{- if .Values.networkPolicy.enabled }}
---
# Network Policy for main application
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "tradinggoose.fullname" . }}-app
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.app.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "tradinggoose.app.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from realtime service
  {{- if .Values.realtime.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.realtime.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.app.service.targetPort }}
  {{- end }}
  # Allow ingress from ingress controller
  {{- if .Values.ingress.enabled }}
  - from: []
    ports:
    - protocol: TCP
      port: {{ .Values.app.service.targetPort }}
  {{- end }}
  # Allow custom ingress rules
  {{- with .Values.networkPolicy.ingress }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  egress:
  # Allow egress to PostgreSQL
  {{- if .Values.postgresql.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.postgresql.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow egress to realtime service
  {{- if .Values.realtime.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.realtime.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.realtime.service.targetPort }}
  {{- end }}
  # Allow egress to Ollama
  {{- if .Values.ollama.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.ollama.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.ollama.service.targetPort }}
  {{- end }}
  # Allow egress to Kronos
  {{- if .Values.kronos.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.kronos.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.kronos.service.targetPort }}
  {{- end }}
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow HTTPS egress for external APIs
  - to: []
    ports:
    - protocol: TCP
      port: 443
  # Allow custom egress rules
  {{- with .Values.networkPolicy.egress }}
  {{- toYaml . | nindent 2 }}
  {{- end }}

{{- if .Values.realtime.enabled }}
---
# Network Policy for realtime service
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "tradinggoose.fullname" . }}-realtime
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.realtime.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "tradinggoose.realtime.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from main application
  - from:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.realtime.service.targetPort }}
  # Allow ingress from ingress controller
  {{- if .Values.ingress.enabled }}
  - from: []
    ports:
    - protocol: TCP
      port: {{ .Values.realtime.service.targetPort }}
  {{- end }}
  egress:
  # Allow egress to PostgreSQL
  {{- if .Values.postgresql.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.postgresql.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow HTTPS egress for external APIs
  - to: []
    ports:
    - protocol: TCP
      port: 443
{{- end }}

{{- if .Values.postgresql.enabled }}
---
# Network Policy for PostgreSQL
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "tradinggoose.fullname" . }}-postgresql
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.postgresql.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "tradinggoose.postgresql.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from main application
  - from:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  # Allow ingress from realtime service
  {{- if .Values.realtime.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.realtime.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow ingress from migrations job
  {{- if .Values.migrations.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.migrations.labels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  egress:
  # Allow minimal egress (for health checks, etc.)
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
{{- end }}

{{- if .Values.ollama.enabled }}
---
# Network Policy for Ollama
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "tradinggoose.fullname" . }}-ollama
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.ollama.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "tradinggoose.ollama.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from main application
  - from:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.ollama.service.targetPort }}
  egress:
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow HTTPS egress for model downloads
  - to: []
    ports:
    - protocol: TCP
      port: 443
{{- end }}

{{- if .Values.kronos.enabled }}
---
# Network Policy for Kronos
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "tradinggoose.fullname" . }}-kronos
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tradinggoose.kronos.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "tradinggoose.kronos.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from main application
  - from:
    - podSelector:
        matchLabels:
          {{- include "tradinggoose.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.kronos.service.targetPort }}
  egress:
  # Weights are baked into the image, so only DNS is needed
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
{{- end }}
{{- end }}